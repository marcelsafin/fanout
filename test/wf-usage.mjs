export const meta = {
  name: 'wf-usage',
  description: 'replay captured real copilot JSONL: assistant text + summed data.outputTokens',
}
const text = await agent('STUB_COPILOT_EVENTS')
return { text, spent: budget.spent() }
